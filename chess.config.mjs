/** @type {import('next').NextConfig} */
const nextConfig = {
    swcMinify: true,
    reactStrictMode: true,
    images: {
      domains: ['placeholder.svg'],
    },
  };
  
  export default nextConfig;
  